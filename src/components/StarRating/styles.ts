import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
  },
  starsContainer: {
    flexDirection: "row",
    gap: 8,
  },
  ratingText: {
    marginTop: 8,
    color: "#FFD700",
    fontSize: 14,
    fontWeight: "600",
  },
});
