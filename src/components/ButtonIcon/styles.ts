import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 10,
        width: '100%',
        backgroundColor: '#FAA916',
        borderRadius: 25,
        alignItems: "center",
    },
    title: {
        fontSize: 14,
        fontWeight: 'bold',
        color: '#313D49',
    },
    group: {
        flexDirection: "row",
        alignItems: "center",
        gap: 10,
    }
});